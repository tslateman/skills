.DS_Store
node_modules/
.entire/
*.log
__pycache__/
*.pyc

# slop-check's clean-draft fixture is real unedited writing about internal
# systems. It stays on disk and out of the published repo.
# Path-independent so a group reshuffle cannot silently un-ignore it.
**/slop-check/evals/
