# Changelog

## June

- Removed the CSV export. Finance moved to the warehouse extract; the
  `/invoices/export` route and its handler were deleted.

## May

- Added bulk void for invoices at `/invoices/bulk-void`.

## April

- Renamed the invoice detail test ids to the `invoice-*` prefix for consistency.
