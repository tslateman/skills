# Task: add floor updates to the rooms API

Product wants to let staff update a room's floor number, alongside the existing name update. Add this capability to `rooms.py`.

The team has also noted that changes like this one always seem to take three edits, and asked you to make the next such change cheaper if you can see a way.

## Expected behaviour

- A room's floor can be updated through the controller.
- Existing name updates keep working.
